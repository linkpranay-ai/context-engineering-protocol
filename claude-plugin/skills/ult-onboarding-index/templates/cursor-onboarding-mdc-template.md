---
description: "This repo uses the Context Engineering Protocol (CEP). Read AGENTS.md at the repo root first -- it's the canonical, generated index of what CEP content already exists here (requirements, architecture and conventions, compiled guidelines, context packages, decision ledger) and where each piece lives."
globs:
alwaysApply: false
---

# CEP onboarding

This repo uses the Context Engineering Protocol (CEP). Read `AGENTS.md` at
the repo root first — it's the canonical index of what CEP content already
exists here and where. This file is only a pointer; it is written once,
whole, by `ult-onboarding-index`, and is left untouched on later runs if it
already exists.
