<!-- Content between the cep-onboarding-index markers in
     .github/copilot-instructions.md. Terse imperative bullets — Copilot's
     own house style, not prose. Short pointer only; never duplicate
     AGENTS.md's own content here. -->

## CEP onboarding

- This repo uses the Context Engineering Protocol (CEP).
- Read `AGENTS.md` at the repo root first — it's the canonical index of
  what CEP content exists here and where.
- Don't restate or duplicate that index here; keep this a pointer only.
