context_package:
  id: sparkline-baseline-marker_feature-add_20260724
  task: >
    Add an optional baseline marker to the Sparkline renderable: when a
    dataset contains both positive and negative values, draw a visually
    distinguishable row/column at the zero value so the sign change is
    visible at a glance.
  scope:
    feature: sparkline baseline marker
    task_type: feature-add
    new_or_existing: existing
    out_of_scope: >
      Not touching the public Sparkline WIDGET's CSS/component-class API
      (src/textual/widgets/_sparkline.py) in this pass — scoped to the
      renderable (src/textual/renderables/sparkline.py) only. (self-answered,
      no live interactive user in this dogfood run — flagged as simulated,
      same convention as the prior disabled-widget-focusable run.)
  aspects:
    - aspect_id: 1
      name: sparkline baseline marker
      what_l3_covered: true
      what_l2_covered: false
      what_l1_covered: false
      llm_knowledge_used: false
      web_fallback_used: false
  generated_at: 2026-07-24T00:00:00Z
  human_approved: true
  content_hash: 62a9b119
  what_l1_fallback_count: 0
  llm_knowledge_count: 0
  web_fallback_count: 0
  conflicts_count: 0
  llm_scaffold_count: 0
  org_convention_ref: org-conventions/feature-add.yaml

  summary:
    - "What-L3: Sparkline is a single, self-contained renderable class (src/textual/renderables/sparkline.py:20), degree 7 in the code graph — only connections are to Style and its own four methods."
    - "What-L2: zero matches for 'sparkline' anywhere in docs/guide/ (both a direct grep and an md_index.py query against the built l2_index.json returned nothing) — this is an undocumented-existing-behavior gap (L2 gap, L3 covered), not a both-layers-gap."
    - "No conflicts detected — there is nothing to contradict with, since What-L2 has no coverage at all for this module."
    - "Blast radius (graphify affected) is minimal and generic: the only dependent is textual/widgets/__init__.py's shared lazy-import __getattr__ (L102), the same registration point every widget in the package goes through — not a Sparkline-specific dependent."
    - "This run is the case study's deliberate negative control: the task and module were chosen for exactly this profile."

  context_items:
    - id: ctx_001
      layer: what-l3
      source: "src/textual/renderables/sparkline.py:20 (Sparkline class)"
      type: implementation
      confidence: EXTRACTED
      aspect_id: 1
      aspect: sparkline baseline marker
      summary: >
        `graphify explain "Sparkline"` resolves unambiguously to
        `renderables_sparkline_sparkline`, degree 7. Connections: uses
        `rich.style.Style` (inferred), and its own `__init__`,
        `__rich_console__`, `_buckets`, `__rich_measure__` methods
        (extracted). No connection to any other product module. Note:
        `graphify query "Sparkline"` (broad BFS) returns 96 mostly-unrelated
        nodes (Style, DataTable, OptionList, TextArea, ...) due to a real
        same-name collision with the widget wrapper class of the same name in
        src/textual/widgets/_sparkline.py — graphify explain's exact-label
        resolution was required to get a usable result, per the
        corroboration-gate guidance already in this skill's Step 4.
      what_l1_fallback: false

    - id: ctx_002
      layer: what-l3
      source: "src/textual/renderables/sparkline.py (render loop, lines ~90-135)"
      type: implementation
      confidence: EXTRACTED
      aspect_id: 1
      aspect: sparkline baseline marker
      summary: >
        The render loop (`__rich_console__`) buckets `self.data` into
        `width` partitions, computes `minimum`/`maximum` across the whole
        dataset once, and maps each bucket's summary value to a bar-height
        index via `(partition_summary - minimum) / extent`. There is no
        existing concept of a "zero" or "baseline" value anywhere in this
        method — colour and height are both purely relative to the
        dataset's own min/max, so a literal-zero baseline marker is a new
        concept, not an extension of an existing one.
      what_l1_fallback: false

    - id: ctx_003
      layer: what-l3
      source: "graphify affected \"renderables_sparkline_sparkline\" --depth 2"
      type: blast-radius
      confidence: EXTRACTED
      aspect_id: 1
      aspect: sparkline baseline marker
      summary: >
        Resolved-node query returns "No affected nodes found." Per this
        skill's Step 4.5 fallback rule (degree ≤ 5 typically means a thin
        wrapper, so don't trust an empty result at face value), pivoted to
        the structurally-related widget class
        (src/textual/widgets/_sparkline.py:17, resolved node ID
        `src_textual_widgets_sparkline_py_widgets_sparkline_sparkline`,
        also degree 7). That query returns exactly one dependent:
        `textual/widgets/__init__.py:102`'s `__getattr__` lazy-loader, which
        `uses`/`references` every widget class in the package generically
        (confirmed by reading the source — it is the shared
        `_WIDGETS_LAZY_LOADING_CACHE` mechanism, not Sparkline-specific).
        This is a genuine, real "self-contained, no feature-specific
        dependents" finding — the fallback rule's second empty-result check
        was satisfied honestly, not skipped.
      what_l1_fallback: false

    - id: ctx_004
      layer: constraints
      source: "starter_kit/project_guidelines/COMPILED-GUIDELINES.md (Global)"
      type: constraint
      constraint_class: convention
      scope: Global
      confidence: EXTRACTED
      summary: >
        Add docstrings in the codebase's existing style; write tests for
        every code change; format with black before opening a PR; update
        CHANGELOG.md before opening a PR; full test suite must pass before a
        PR is opened. No `### Scope:` section in COMPILED-GUIDELINES.md
        matches `src/textual/renderables/**` — only `tests/snapshot_tests/**`
        has a scoped entry, which does not apply here since this task does
        not touch snapshot tests.
      what_l1_fallback: false

    - id: ctx_005
      layer: constraints
      source: "starter_kit/project_guidelines/COMPILED-GUIDELINES.md (Global)"
      type: constraint
      constraint_class: compliance
      scope: Global
      confidence: EXTRACTED
      summary: >
        AI-generated PRs are accepted only if the PR fills in the repo's PR
        template, self-identifies as AI-generated naming the agent used, and
        links to an issue/discussion already approved by a maintainer
        (@willmcgugan). Not applicable to this dogfood run itself (no PR is
        being opened against Textualize/textual), but would gate any real
        follow-through on this task.
      what_l1_fallback: false

  conflicts_detected: []

  gaps_detected:
    - aspect_id: 1
      aspect: sparkline baseline marker
      layers_checked: [what-l2, what-l3]
      what_l1_fallback_used: false
      llm_knowledge_used: false
      web_fallback_used: false
      note: >
        What-L2 gap only (not both-layers-gap — What-L3 is covered, so Step
        7.1/7.2 do not trigger). docs/guide/ has zero mentions of Sparkline
        in any form; there is no documented behavior to contradict or
        extend. This is expected for a small, self-contained renderable that
        was likely never significant enough to warrant its own guide page.

  open_questions: []

  decisions_log:
    - topic: "Whether the baseline marker should be a default-on or opt-in behavior"
      decided: "Opt-in (new constructor parameter, default preserves current rendering) — self-resolved, no live user available in this dogfood run, same convention as the prior disabled-widget-focusable package."

  non_regression_risks:
    - "Sparkline.__rich_console__: must keep producing exactly the same output for any call that does not opt into the new baseline-marker parameter — existing tests in tests/renderables/test_sparkline.py and the three committed snapshot SVGs (test_sparkline.svg, test_sparkline_component_classes_colors.svg, test_sparkline_render.svg) must keep passing unchanged."
