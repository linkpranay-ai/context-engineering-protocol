<!-- Content between the cep-onboarding-index markers in AGENTS.md. Delete
     every section below whose slot existence-checked false in Step 2 --
     never link to something that isn't actually there. -->

## CEP onboarding index

This repo uses the [Context Engineering Protocol](https://github.com/linkpranay-ai/context-engineering-protocol)
(CEP). The links below point at what's already generated — run the
matching CEP skill (named in parens) to fill in anything missing.

### Architecture & Conventions (How-L2)

<!-- Include only if any How-L2 artifact (CODING-STANDARDS.md,
     TESTING-GUIDELINES.md, interfaces/*.md, per-module CONTEXT.md) was
     found present. List only the kinds actually found. -->

- `<resolved-how-l2-path>/CODING-STANDARDS.md` — coding conventions.
- `<resolved-how-l2-path>/TESTING-GUIDELINES.md` — testing conventions.
- `<resolved-how-l2-path>/interfaces/` — module interface-boundary docs.
- `<resolved-how-l2-path>/CONTEXT.md` files — per-module architecture notes.

### Compiled guidelines

<!-- Include only if compiled_guidelines existence-checked true. -->

- `<resolved-compiled-guidelines-path>` — reconciled team conventions from
  every scattered source `compiling-project-guidelines` found. (generated by
  `compiling-project-guidelines`)

### Scaffolded content index

<!-- Include only if autoscaffold_content_index existence-checked true.
     Link only -- CEP-INDEX.md is already its own router, don't
     re-enumerate its contents here. -->

- `<resolved-autoscaffold-content-index-path>` — full router of everything
  `ult-autoscaffold-content` has generated so far. (generated by
  `ult-autoscaffold-content`)

### Context packages

<!-- Include only if context_packages existence-checked true. -->

- `<resolved-context-packages-path>` — assembled context packages for
  specific tasks/features. (generated by `ult-context-generate`)

### Decision ledger

<!-- Include only if decision_ledger existence-checked true. Link only --
     it's JSON, don't parse or summarize entries here. -->

- `<resolved-decision-ledger-path>` — recorded architectural decisions and
  their rationale. (generated by `ult-institutional-memory-distill`)

<!-- Any outputs-bucket slot (plans_output, brainstorm_output,
     user_stories_output, security_docs, security_report,
     project_plan_docs) that existence-checked true gets one more bullet
     here, same shape as above -- resolved path, one-sentence description,
     producing skill in parens. Omit entirely if none of them resolved. -->
